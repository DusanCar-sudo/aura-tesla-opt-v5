const layers={meter:{symbol:'α',title:'See the whole supply.',description:'The Shelly Pro 3EM provides the three-phase metering layer: the starting point for understanding consumption across the incoming phases.',component:'Shelly Pro 3EM',role:'Three-phase energy measurement',connection:'Local Ethernet network'},compute:{symbol:'β',title:'Give intelligence a home.',description:'A fanless MeLE mini-PC provides the local computing foundation. The eight-port Ethernet switch brings the connected components onto one network.',component:'MeLE Quieter3Q N5105',role:'Local computing foundation',connection:'Eight-port Ethernet backbone'},control:{symbol:'γ',title:'Four channels. Clear roles.',description:'Four Shelly Pro 1PM modules form the load-control layer. The design assigns channels to water heating, space heating, EV charging and an auxiliary load; final interfaces and load ratings need engineering review.',component:'4 × Shelly Pro 1PM',role:'Four designated switching channels',connection:'Ethernet-connected relays'},backup:{symbol:'δ',title:'Keep the core supported.',description:'A 24 V supply, DIN-rail UPS module and LiFePO4 battery form the proposed backup path. A DC–DC converter supplies the low-voltage computing layer. Backup duration has not been validated.',component:'24 V LiFePO4 + DR-UPS40',role:'Proposed low-voltage backup',connection:'24 V supply / DC–DC conversion'}};
const tabs=[...document.querySelectorAll('[role=tab]')];
function activate(tab){tabs.forEach(t=>{const selected=t===tab;t.setAttribute('aria-selected',String(selected));t.tabIndex=selected?0:-1});const l=layers[tab.dataset.layer],panel=document.getElementById('layer-content');panel.setAttribute('aria-labelledby',tab.id);panel.innerHTML=`<h3>${l.title}</h3><p>${l.description}</p><dl><div><dt>Component</dt><dd>${l.component}</dd></div><div><dt>Role</dt><dd>${l.role}</dd></div><div><dt>Connection</dt><dd>${l.connection}</dd></div></dl>`}
tabs.forEach((tab,i)=>{tab.addEventListener('click',()=>activate(tab));tab.addEventListener('keydown',e=>{let next;if(e.key==='ArrowRight')next=(i+1)%tabs.length;if(e.key==='ArrowLeft')next=(i+tabs.length-1)%tabs.length;if(e.key==='Home')next=0;if(e.key==='End')next=tabs.length-1;if(next!==undefined){e.preventDefault();activate(tabs[next]);tabs[next].focus()}})});
const menu=document.querySelector('.menu'),nav=document.querySelector('nav');function closeMenu(){nav.classList.remove('open');menu.setAttribute('aria-expanded','false')}menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',closeMenu));document.addEventListener('keydown',e=>{if(e.key==='Escape'&&nav.classList.contains('open')){closeMenu();menu.focus()}});
const body=document.getElementById('parts-body'),filter=document.getElementById('part-filter'),status=document.getElementById('parts-status');let parts=[];
function renderParts(){const shown=parts.filter(p=>filter.value==='all'||p.Category===filter.value);body.replaceChildren(...shown.map(p=>{const tr=document.createElement('tr');[p.Name,p['Product Name']||'Custom mounting component',p.Quantity,new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(Number(p['Total Cost']))].forEach(value=>{const td=document.createElement('td');td.textContent=value;tr.append(td)});return tr}));status.textContent=`Showing ${shown.length} of ${parts.length} components.`}
status.textContent='Loading the parts list…';filter.disabled=true;fetch('assets/parts.json').then(r=>{if(!r.ok)throw Error('Unable to load');return r.json()}).then(data=>{parts=data;renderParts();filter.disabled=false}).catch(()=>{status.textContent='The interactive list could not load. Use “Download the parts list” above to inspect the CSV.'});filter.addEventListener('change',renderParts);
